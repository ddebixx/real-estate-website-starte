query GetPosts {
  posts(orderBy: postPublishDate_DESC) {
    id
    postPublishDate
    postTitle
    postSlug
    postContent {
      html
    }
    author {
      authorName
      authorPhoto {
        url
      }
    }
  }
}
